version: '3'
services:
  hydra:
    image:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/code
      - logvolume01:/var/log
    links:
      - redis
  postgres:
    image: postgres
volumes:
  logvolume01: {}
